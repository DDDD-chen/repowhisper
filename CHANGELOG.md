# Changelog

## 0.2.0

- Add diff-focused briefs with `--diff` and optional `--base`
- Add `--copy` for clipboard handoff into coding-agent chats
- Improve README first-screen positioning and demo preview
- Keep changed files plus core repository context when generating diff briefs

## 0.1.0

- Initial CLI for Markdown and JSON repository briefs
- Local scanner with generated/dependency folder filtering
- Stack, command, test, CI, doc, and entrypoint detection
- Optional `AGENTS.md` generation
- Hourly GitHub star watcher script
